version: "3"

services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile.client.prod
    container_name: dvholidays-client-stage
    networks:
      - dvholidays-nerwork

  nginx:
    image: nginx:1.15.12-alpine
    container_name: dvholidays-nginx-stage
    restart: unless-stopped
    ports:
      - 8080:80
    volumes:
      - ./nginx/dvholidays.stage.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - client
    networks:
      - dvholidays-nerwork

networks:
  dvholidays-nerwork:
    driver: bridge
